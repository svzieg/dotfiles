set record    = "+Photography/Sent"
set spoolfile = "+Photography/INBOX"
set postponed = "+Photography/Drafts"
# set header cache
# set header_cache=~/Localmail/hcache
# needed for maildir format
set mbox_type=Maildir
set sendmail      = "/usr/bin/msmtp -a photography"
set from = "sven.ziegler@svenziegler.photography"
